@using Vamos_Sergy.Models.Items
@model Equipment
<a id="@ViewData["eq"]"
   tabindex="0"
   role="button"
   data-bs-toggle="popover"
   title="@Model.Item.Name"
   data-bs-content="@Model.ToString()"
   href="#">
    <img class="EquipmentImg" src="@Url.Action("GetInventoryImage", "Main", new { id = Model.ItemId})" />
</a>
